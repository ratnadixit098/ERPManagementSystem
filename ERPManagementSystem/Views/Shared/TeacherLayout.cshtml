<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Teacher Portal</title>

    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <script src="https://code.jquery.com/jquery-3.7.0.min.js"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
    <script src="~/Scripts/bootstrap.bundle.min.js"></script>

    <style>
        :root {
            --sidebar-width: 260px;
            --bg-dark: #020617;
            --card-dark: rgba(15, 23, 42, 0.9);
            --accent-teacher: #6366f1; /* Indigo Accent for Teachers */
            --accent-purple: #a855f7;
        }

        body {
            margin: 0;
            font-family: 'Plus Jakarta Sans', sans-serif;
            background-color: var(--bg-dark);
            background-image: radial-gradient(at 0% 0%, rgba(99, 102, 241, 0.05) 0, transparent 50%);
            color: #f1f5f9;
            min-height: 100vh;
            overflow-x: hidden;
        }

        /* ✨ Glassmorphism Header */
        .header {
            height: 70px;
            background: rgba(15, 23, 42, 0.8);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 30px;
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
            position: sticky;
            top: 0;
            z-index: 1000;
        }

            .header h2 {
                margin: 0;
                font-size: 20px;
                font-weight: 700;
                background: linear-gradient(to right, #fff, var(--accent-teacher));
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
            }

        /* 👤 User Capsule */
        .user-menu {
            position: relative;
            cursor: pointer;
            background: rgba(255, 255, 255, 0.03);
            padding: 8px 18px;
            border-radius: 12px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            transition: 0.3s;
            display: flex;
            align-items: center;
            gap: 12px;
        }

            .user-menu:hover {
                background: rgba(99, 102, 241, 0.1);
                border-color: var(--accent-teacher);
            }

        .dropdown {
            display: none;
            position: absolute;
            top: 60px;
            right: 0;
            background: #0f172a;
            border-radius: 12px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
            min-width: 180px;
            z-index: 1100;
            border: 1px solid rgba(255,255,255,0.1);
            padding: 8px;
        }

            .dropdown a {
                display: flex;
                align-items: center;
                gap: 10px;
                padding: 10px 15px;
                text-decoration: none;
                color: #94a3b8;
                font-weight: 500;
                border-radius: 8px;
                transition: 0.2s;
            }

                .dropdown a:hover {
                    background: rgba(239, 68, 68, 0.1);
                    color: #ef4444;
                }

        /* 🚀 Sidebar Styling */
        .main-wrapper {
            display: flex;
        }

        .sidebar {
            width: var(--sidebar-width);
            height: calc(100vh - 70px);
            background: linear-gradient(180deg, rgba(15, 23, 42, 0.95), rgba(2, 6, 23, 1));
            border-right: 1px solid rgba(255, 255, 255, 0.05);
            padding: 25px 15px;
            position: fixed;
            left: 0;
        }

            .sidebar h3 {
                font-size: 11px;
                text-transform: uppercase;
                letter-spacing: 1.5px;
                color: var(--accent-teacher);
                font-weight: 700;
                margin-bottom: 25px;
                padding-left: 10px;
            }

            .sidebar a {
                display: flex;
                align-items: center;
                gap: 12px;
                padding: 12px 15px;
                color: #94a3b8;
                text-decoration: none;
                font-size: 14px;
                font-weight: 500;
                border-radius: 12px;
                margin-bottom: 8px;
                transition: 0.3s;
            }

                .sidebar a i {
                    width: 20px;
                    font-size: 16px;
                }

                .sidebar a:hover {
                    background: rgba(255, 255, 255, 0.03);
                    color: #fff;
                    transform: translateX(5px);
                }

                .sidebar a.active {
                    background: linear-gradient(90deg, rgba(99, 102, 241, 0.1), transparent);
                    color: var(--accent-teacher);
                    border-left: 3px solid var(--accent-teacher);
                }

        /* 🌑 Content Area */
        .content {
            flex: 1;
            margin-left: var(--sidebar-width);
            padding: 40px;
            min-height: calc(100vh - 70px);
            background: radial-gradient(at 50% 50%, rgba(99, 102, 241, 0.02) 0, transparent 80%);
        }

        /* Custom Scrollbar */
        ::-webkit-scrollbar {
            width: 5px;
        }

        ::-webkit-scrollbar-track {
            background: var(--bg-dark);
        }

        ::-webkit-scrollbar-thumb {
            background: #1e293b;
            border-radius: 10px;
        }
    </style>
</head>

<body>

    <div class="header">
        <h2><i class="fa fa-chalkboard-teacher text-primary me-2"></i> Faculty Panel</h2>

        <div class="user-menu" id="userMenu">
            <div style="width: 32px; height: 32px; background: linear-gradient(135deg, var(--accent-teacher), var(--accent-purple)); border-radius: 8px; display: flex; align-items: center; justify-content: center; font-weight: bold; font-size: 14px; color: white;">
                @Session["UserName"].ToString().Substring(0, 1).ToUpper()
            </div>
            <span>@Session["UserName"]</span>
            <i class="fa fa-chevron-down small text-muted"></i>

            <div class="dropdown" id="dropdown">
                <a href="/Login/Logout">
                    <i class="fas fa-power-off"></i> Logout
                </a>
            </div>
        </div>
    </div>

    <div class="main-wrapper">

        <div class="sidebar">
            <h3>Faculty Menu</h3>

            <a href="#" class="active">
                <i class="fa fa-th-large"></i> Dashboard
            </a>

            <a href="/Attendance/TimeTable">
                <i class="fa fa-calendar-check"></i> Mark Attendance
            </a>

            <a href="/Commitment/Create">
                <i class="fa fa-handshake"></i> Fee Commitment
            </a>

            <a href="#">
                <i class="fa fa-book-open"></i> Study Material
            </a>

            <a href="#">
                <i class="fa fa-clipboard-list"></i> Exam Duties
            </a>

            <div style="margin-top: 50px; padding: 20px; background: rgba(99, 102, 241, 0.03); border-radius: 16px; border: 1px solid rgba(99, 102, 241, 0.1);">
                <p class="small text-muted mb-2">Notice</p>
                <small class="text-white-50">Faculty meeting today at 4:00 PM in Hall A.</small>
            </div>
        </div>

        <div class="content">
            @RenderBody()
        </div>

    </div>

    <script>
        $(document).ready(function () {
            // Dropdown logic
            $('#userMenu').on('click', function (e) {
                e.stopPropagation();
                $('#dropdown').fadeToggle(150);
            });

            $(document).on('click', function () {
                $('#dropdown').fadeOut(150);
            });

            // Path highlighting
            var currentPath = window.location.pathname;
            $('.sidebar a').each(function () {
                if ($(this).attr('href') === currentPath) {
                    $('.sidebar a').removeClass('active');
                    $(this).addClass('active');
                }
            });
        });
    </script>

</body>
</html>